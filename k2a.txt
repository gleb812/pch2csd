In/Out - Never change
Osc 
	OscA, OscB, OscC, OscPM, 
	OscShpA, OscShpB, OscDual, OscPerc 
			Pitch & Pitch Mod
			default: k
			turns a, when a (Pitch OR Pitch Mod)

	OscNoise
			 
	 		Pitch & Pitch Mod & Width
			default: k
			turns a, when a (Pitch OR Pitch Mod OR Width)

	
	MetNoise
			 
	 		FreqMod & ColorMod
			default: k
			turns a, when a (FreqMod OR ColorMod)

	DrumSynth, OscString, DXs - Never change

	OscMaster
			Pitch, PitchMod, Output
			default: k
			turns a, when a (Pitch OR Pitch Mod)

Rnd
	RandomA, RandomB - Never change
	
	RndClkA, RndClkB
			
			Clk, Rst, Seed, Out
			default: k (yellow)
			turns a, when a (Clk OR Rst OR Seed)

	RndTrig
			Clk, Rst, Seed, Prob, Out
			default: k (yellow)
			turns a, when a (Clk OR Rst OR Seed OR Prob)

	RndPattern
			
			Clk, Rst
			default: k (yellow)
			turns a, when a (Clk OR Rst)


Filter
	FltLP
			Mod
			default: k 
			turns a, when a (Mod)


	FltHP - Never changes (SO STRANGE!!!!)
	FltStatic, WahWah, FltVoice, all Eqs, Vocoder - Never change
	
	FltNord
			Pitch, PitchMod, FM, Res
			default: k 
			turns a, when a (Pitch OR PitchMod OR FM OR Res)

	FltClassic, FltMulti, FltPhase, FltComb
			
			Pitch, PitchMod
			default: k 
			turns a, when a (Pitch OR PitchMod)

Delay
	
	Never changes, except
	
	DlyShiftReg, DlyClock	
			Everything defaults k
			Changes to a, when at least 1 is a

	DelayB
			FBMod, DWMod
			default: k 
			turns a, when a (FBMod OR DWMod)


Level
	
	Never changes, except

	LevAdd, LevConv, LevAmp, LevMulti, LevMod, CompLev,
	CompSig, MinMax, ModAmt
			Everything defaults k
			Changes to a, when at least 1 is a

	EnvFollow
			Input
			default: k 
			turns a, when a
	
	NoiseGate	
			Input, Output
			default: k 
			turns a, when a

	
Switch
	Everything, except Mux8-1, Mux1-8, Mux8-1x1	
			Inputs, (Outputs, except Ctrl Output - Never change)
			default: k 
			turns a, when a on Inputs

	Mux8-1, Mux1-8, Mux8-1x1
			(Inputs except Ctrl - Never change), Outputs
			default: k 
			turns a, when a on Inputs



Seq

	SeqEvent
			Clk, Out1, Out2
			default: k (yellow)
			turns a, when a (Clk)	
			
	SeqVal
			Clk, InVal, OutVal, Out
			default: k 
			turns a, when a (Clk OR InVal)	
	
	SeqLev, SeqNote, SeqCtr
			Everything defaults k
			Changes to a, when at least 1 is a


Note

	No changes, except

	Glide
			Everything defaults k
			Changes to a, when at least 1 is a

	PitchTracker
			Input, Outs: Period, Gate
			default: k 
			turns a, when a (Input)

	ZeroCnt
			Input
			default: k 
			turns a, when a

	LevScaler
			Input, Out
			default: k 
			turns a, when a (Input)

LFO Never Change

Env
	Everything
			Input, Out
			default: k 
			turns a, when a (Input)

FX 
	No changes, except
	
	Digitizer
			Everything
			default: k 
			turns a, when a (Input OR Rate)

Shaper & Mixer & Logic
	Everything
			Everything defaults k
			Changes to a, when at least 1 is a

MIDI never change
	
	
	
	
	
			
	 
	

			
	
			
